# should be the result of parsing "{{ name | trim }}"
DocumentNode:
   children:
      - FilteredValueNode:
         value:
            VariableNode:
               value: name
         filters:
            - FilterNode:
               identifier: trim

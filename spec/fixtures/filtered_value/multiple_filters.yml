# should be the result of parsing "{{ name | trim | upcase }}"
DocumentNode:
   children:
      - InjectNode:
            value:
               FilteredValueNode:
                  value:
                     VariableNode:
                        value: name
                  filters:
                     - FilterNode:
                        identifier: trim
                     - FilterNode:
                        identifier: upcase
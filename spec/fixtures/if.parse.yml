# {% if foo %}bar{% endif %}
DocumentNode:
  children:
    - IfNode:
         expression:
            VariableNode:
               value: "foo"
         true_children:
            - TextNode:
               text: "bar"
         false_children: []
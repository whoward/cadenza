# should be the result of parsing "{{ a + b * c }}"
DocumentNode:
  children:
    - InjectNode:
        value:
          BooleanNode:
            operator: "+"
            left:
              VariableNode:
                value: a
            right:
              BooleanNode:
                operator: "*"
                left:
                  VariableNode:
                    value: b
                right:
                  VariableNode:
                    value: c

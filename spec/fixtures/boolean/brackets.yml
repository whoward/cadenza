# should be the result of parsing "{{ (a + b) * c }}"
DocumentNode:
  children:
    - InjectNode:
        value:
          BooleanNode:
            operator: "*"
            right:
              VariableNode:
                value: c
            left:
              BooleanNode:
                operator: "+"
                left:
                  VariableNode:
                    value: a
                right:
                  VariableNode:
                    value: b

# {% for foo in bar %}{% endfor %}
DocumentNode:
  children:
    - ForNode:
         iterator:
            VariableNode:
               value: "foo"
         iterable:
            VariableNode:
               value: "bar"
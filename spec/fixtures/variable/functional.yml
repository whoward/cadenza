# should be the result of parsing "{{ load 'mytemplate' }}"
DocumentNode:
   children:
      - VariableNode:
         value: load
         parameters:
            - ConstantNode:
               value: mytemplate
